<%- contentFor('html_attribute') %>

<%- contentFor('extra_css') %>

<%- contentFor('body_attribute') %>

<%- contentFor('content') %>

<div class="row">
  <div class="col-12">
    <div class="card">
      <div class="card-header border-bottom border-dashed d-flex align-items-center justify-content-between">
        <h4 class="header-title mb-0">Servizi</h4>
        <span class="text-muted fs-13">Listino: <strong>Listino Prezzi 2025</strong></span>
      </div>

      <div class="card-body">
        <div id="servizi-status" class="text-muted mb-3">Caricamento...</div>

        <div class="table-responsive">
          <table class="table table-striped table-hover align-middle mb-0">
            <thead>
  <tr>
    <th>Nome</th>
    <th>Codice</th>
    <th>Famiglia</th>
    <th>Prezzo</th>
    <th>Attivo</th>
    <th>Creato il</th>
  </tr>
</thead>
            <tbody id="servizi-tbody"></tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>

<%- contentFor('extra_javascript') %>
<script src="/js/pages/apps-service.js"></script>